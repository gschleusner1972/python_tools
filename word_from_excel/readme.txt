The Excel file is named data.xlsx and is located in the same folder as the script.
The Word template is named template.docx and is also located in the same folder.
There's a placeholder in the Word document for each column in the Excel file, formatted as [[column_name]].
To run the script, simply execute it, and it will generate Word documents in the output folder.